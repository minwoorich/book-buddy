# 2026-09-14 개발일지

## 한 일 (9/13 밤~9/14)

- **구현 완료**: 계획의 22개 태스크 전부 + 알라딘→네이버 교체(4R) — 서브에이전트 방식(태스크별 구현→리뷰→픽스 루프)으로 진행
- 태스크별 리뷰에서 잡힌 주요 실결함(모두 수정 완료):
  - 랭킹/통계의 부서·팀 그룹핑이 회사를 넘어 병합되던 문제 (연구소가 바텍/레이언스 양쪽에 존재)
  - SQLite UTC 타임스탬프를 로컬로 오파싱해 완독일이 하루 밀리던 문제 → `parseDbDate` 공용 헬퍼
  - 예약 "N순위"가 실제 대기열 순번이 아니던 문제 → queue_rank 서브쿼리
  - 예약자 보류 상태에서 잘못된 [대출하기] 버튼 노출
  - 장소 지도 (0,0) "Null Island" 마커 버그
- 알라딘 API 종료 → 네이버 책 검색으로 전면 교체 ([[../decisions/2026-09-13-알라딘-네이버-교체|결정]])
- 테스트 25개 green (loanService 규칙 9개 + parseAiAnswer 7 + parsePlaceRanking/mergeRanking 7 + db 2)
- 프로덕션 빌드(`npm run build`) 성공 + 부팅 확인. Dockerfile/README 작성

## 지금 상태

- 최종 전체 브랜치 리뷰 진행 중 (feat/book-buddy)
- **차단: API 키** — `.env`에 `NUXT_ANTHROPIC_API_KEY` + `NUXT_NAVER_SEARCH_CLIENT_ID/SECRET`가 없어 시드 라이브 실행·AI 실동작·브라우저 E2E 미검증 ([[../plans/로드맵|로드맵]] 체크리스트)

## 다음 할 일

- 최종 리뷰 결과 픽스 → main 머지
- 민우가 키 넣으면: seed 실행 → AI 챗봇/검색 라이브 확인 → 데모 리허설
