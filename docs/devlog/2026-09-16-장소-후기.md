# 2026-09-16 장소 후기(태그 칩)

## 한 일

- "카카오 리뷰 임베딩" 요청 → 조사(리뷰 API 없음, iframe 비공식) → 자체 태그 후기로 결정 — [[../decisions/2026-09-16-장소-후기-자체구현]]
- `Place.kakaoId/placeUrl` 매핑, `place_reviews` 테이블(1인 1후기 upsert), 입력 검증, 레포, API 3종
- `ReadingPlaceReviewPanel`: 카드 안 집계 칩(많은 순 3개) + 최근 코멘트 2줄 + 인라인 작성/수정/삭제
- "카카오맵에서 보기"는 이제 장소 상세(`place_url`)로 — 카카오 리뷰는 거기서

## 검증

- vitest 신규 13건(kakaoLocalPlace 2 · placeReviewInput 6 · placeReviewRepo 5), 전체 133건 green, `npm run build` 통과
- 빌드 서버 curl 7단계(포트 3100): PUT 201 → GET 요약(tagCounts·recent·mine, 후기 없는 id는 total 0) → 태그 0개 400 → DELETE 204 → 재삭제 404 → 헤더 없이 401 → 잘못된 id 400
- 브라우저 확인은 아직 — 아래 "다음" 참고

## 다음

- **브라우저 E2E 확인 필요**: 작성→집계→수정→삭제 흐름, 검색 변경 시 요약 갱신, 400px 레이아웃, 폴백 예시(키 없음)에서 후기 UI 숨김
- 운영 반영 후 시연 계정으로 후기 몇 건 미리 쌓기(빈 카드보다 집계가 보이는 편이 시연에 좋다)
- 필요해지면: 관리자 삭제, 내 서재 "내가 남긴 장소 후기"

## 메모

- 작업 중 같은 워킹트리에서 다른 세션이 피드 태그 기능(`postTagRepo`·`TagInput.vue`·`PostComposer`)을 동시에 편집 중이었다. 커밋은 내 파일만 골라서 했다.
