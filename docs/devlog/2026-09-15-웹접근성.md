# 2026-09-15 웹 접근성 모드

민우 요청(노인·시각장애 사용자용 큰 글씨·단순 화면, 색약 모드, 다크모드). 결정 배경은 [[decisions/2026-09-15-웹접근성-모드]], 설계는 [[superpowers/specs/2026-09-15-web-accessibility-modes-design]].

## 한 일

- **설정 로직** `app/utils/a11yPrefs.ts` — 순수 함수(정규화·파싱·테마 해석·data-* 반영), TDD로 먼저 테스트 11건 → 구현.
- **컴포저블** `useA11y()` + **복원 플러그인** `restore-a11y.client.ts` (OS 다크모드 변경 추적 포함).
- **UI** `CommonA11yMenu` 팝오버(세그먼트 2개 + 스위치 2개 + 기본값으로, Esc/바깥 클릭 닫힘, role/aria 연결) → 사용자 헤더·관리자 헤더·로그인·회원가입.
- **CSS** `main.css`: 토큰 20개 신설, 다크/색약/색약+다크/단순 화면/글자 크기 계층, 전역 포커스 링. 컴포넌트 25개의 하드코딩 색을 토큰으로 치환(SVG `fill="#…"`는 `style="fill: var(--star)"`로 — 프레젠테이션 속성엔 var()가 안 먹는다).
- **StatusBadge** 색약 모드에서 ● → ✓/✕. 차트 2종은 `--accent-data` 토큰.

## 막힌 것·배운 것

- Bash 히어독으로 .vue를 쓰다 파싱 오류 → Write 도구/파이썬 스크립트로 분리.
- 캡처용 preview 서버(`node .output/server/index.mjs`)를 띄웠는데 3000번에 민우의 `nuxt dev`가 이미 있었다. 캡처는 어느 쪽이든 현재 소스를 반영하므로 결과는 유효. 남의 dev 서버는 건드리지 않음.
- `/calendar`는 `/my`로 합쳐져 있어 달력 단독 캡처는 없음(내 서재 캡처로 대체).

## 다음 할 일

- [ ] 발표자료 "실행가능성(대상 구체화)" 항목에 접근성 모드 스크린샷 매핑
- [ ] 스크린리더 라벨 점검(표지 alt, 아이콘 버튼 aria-label) — 이번엔 시각 모드만 다룸
- [ ] 단순 화면에서 책벗 챗봇 글자 크기(패널 내부는 zoom만 적용됨)
