# 2026-09-16 시연용 게스트 선점 화면 + QR

## 한 일

- **QR 코드**: `scripts/generate-qr.ts`(qrcode 패키지, 오류정정 H) → `design/presentation/qr-vnlibrary.{svg,png}`. 값은 `https://www.vnlibrary.com`.
- **게스트 선점 화면**: 결정은 [[decisions/2026-09-16-시연용-게스트-선점]].
  - DB: `users.is_guest`, `guest_claims(user_id PK, token, claimed_at)` — migrate.ts에 ALTER/CREATE 가드.
  - 서버: `guestRepo`(list/claim/release/releaseAll/resolveUser), `optionalUser`가 게스트면 `x-guest-token` 검증.
    라우트 `GET /api/guests`, `POST /api/guests/:id/claim`(409), `DELETE /api/guests/claim`, `GET /api/guests/me`,
    `POST /api/admin/guests/release-all`.
  - 클라이언트: `useCurrentUser`가 게스트 토큰도 보관(localStorage `bb:guest-token`), `useApi`가 헤더로 실음,
    401 문구를 게스트용으로 분기. `/login` 상단 게스트 카드(3초 폴링, 사용 중 비활성). 헤더 로그아웃이 선점 해제.
    `guest-heartbeat.client.ts`가 게스트만 10초마다 세션 확인. 관리자 회원 페이지에 "게스트 전체 해제" 버튼 + 게스트 뱃지.
  - `scripts/guest-seed.ts`: 게스트 1~N 멱등 생성, `--remove`로 활동 기록까지 삭제.

## 시연 운영 메모

- 켜기: `railway ssh ... -- npx tsx scripts/guest-seed.ts` (게스트 8명). 끄기: 같은 명령에 `--remove`.
- 심사 사이 리셋: 관리자(도서관리자) → 회원 → "게스트 전체 해제". 접속 중인 게스트는 10초 안에 로그아웃된다.
- 게스트가 8명을 넘으면 카드가 전부 "사용 중" — 그때는 기존 로그인 폼(발표자 계정) 또는 회원가입.

## 다음 할 일

- 발표 리허설 때 폰 2대로 동시 선점 → 한쪽 409 안내 확인.
- 시연 종료 후 `--remove`로 게스트 정리.

## 추가 (같은 날) — 다크모드 스위치 + 로그인 안내 문구 정리

- 민우 요청: ① 로그인 화면의 "시연 계정 — 김민우 / 도서관리자" 안내 삭제(심사위원에게 노출될 자리라 부적절) ② 다크모드 스위치를 모든 화면 우측 상단에.
- `CommonThemeToggle` 신규: 기존 접근성 설정(`useA11y` / `bb:a11y`)을 그대로 읽고 쓴다 — 숨겨둔 접근성 메뉴를 되살려도 값이 어긋나지 않는다. `system`이면 OS를 따라 놓이고, 누르면 명시적으로 고정(`nextThemePref`, vitest 2건).
- 배치: AppHeader(로그인/비로그인), AdminHeader, login·signup의 `.a11y-corner`. 모바일(≤640px)은 라벨 숨김.
- 첫 시안은 해·달을 트랙 양끝에 두고 노브가 그 위를 지나가게 했는데, 노브가 **현재 상태 아이콘을 덮어** 보이는 아이콘과 라벨이 서로 반대를 가리켰다 → 아이콘을 노브 안에 넣어 현재 테마를 그대로 보여주도록 수정.
- 검증: vitest 115건 통과, 빌드 통과, 헤드리스 CDP로 클릭 구동 확인(라이트→다크→새로고침 유지→다시 라이트, 홈 헤더 1개, "시연 계정" 문구 0건) + 로그인 라이트/다크·홈 다크·모바일 390px 캡처.

## 추가 (같은 날) — 게스트 슬롯 5 → 8

- 민우 요청: 게스트 계정 3개 더. 심사위원 수가 5명보다 많을 수 있어 여유를 둔다.
- `guest-seed.ts`의 기본 개수만 5 → 8로 올렸다. 시드는 이름(`게스트 N`) 기준 멱등이라 기존 1~5와 선점 중이던 게스트 2의 토큰은 그대로 두고 6·7·8만 새로 들어간다.
- 운영 반영: `railway ssh ... -- npx tsx scripts/guest-seed.ts 8` → "게스트 계정 8개 확인 — 새로 생성 3명". 확인 결과 id 188~195, `guest_claims`는 189 한 건 유지.
- 화면 변경 없음 — `.guest-grid`가 `repeat(auto-fill, minmax(96px, 1fr))`라 8장도 그대로 줄바꿈된다.
